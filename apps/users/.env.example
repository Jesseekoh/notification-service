# ==============================================================================
# Users Service Environment Configuration
# ==============================================================================

# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/notification_users

# Environment mode: development | production
# In development, pino-pretty is enabled and log level is 'debug'.
# In production, structured JSON logs are emitted at 'info' level.
NODE_ENV=development
