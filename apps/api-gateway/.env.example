# ==============================================================================
# API Gateway Environment Configuration
# ==============================================================================

# HTTP Port for the API Gateway (defaults to 3000 if not specified)
PORT=3000

# Environment mode: development | production | test
NODE_ENV=development
